# PubMed API Configuration
# OPTIONAL: Get an API key from: https://www.ncbi.nlm.nih.gov/account/settings/
# Without API key: 3 requests/second, With API key: 10 requests/second
# Leave empty to use without API key (slower but works)
PUBMED_API_KEY=
PUBMED_EMAIL=your-email@example.com
PUBMED_TOOL_NAME=healthcare_data_generator

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
API_DEBUG=True

# Database Configuration (if needed)
DATABASE_URL=sqlite:///./healthcare_data.db

# Logging Configuration
LOG_LEVEL=INFO